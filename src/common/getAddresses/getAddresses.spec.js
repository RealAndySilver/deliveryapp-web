describe('getAddresses section', function () {
    beforeEach(module('appMensajeria.getAddresses'));

    it('should have a dummy test', inject(function() {
        expect(true).toBeTruthy();
    }));
});