describe('completedServices section', function () {
    beforeEach(module('appMensajeria.completedServices'));

    it('should have a dummy test', inject(function() {
        expect(true).toBeTruthy();
    }));
});