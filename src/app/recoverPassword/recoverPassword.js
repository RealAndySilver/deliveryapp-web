(function(module) {

    module.controller('RecoverPasswordController', function () {
        var model = this;

        init();

        function init() {

        }
    });

}(angular.module("appMensajeria.recoverPassword")));