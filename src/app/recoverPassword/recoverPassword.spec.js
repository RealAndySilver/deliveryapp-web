describe('recoverPassword section', function () {
    beforeEach(module('appMensajeria.recoverPassword'));

    it('should have a dummy test', inject(function() {
        expect(true).toBeTruthy();
    }));
});