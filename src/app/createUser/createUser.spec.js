describe('createUser section', function () {
    beforeEach(module('appMensajeria.createUser'));

    it('should have a dummy test', inject(function() {
        expect(true).toBeTruthy();
    }));
});