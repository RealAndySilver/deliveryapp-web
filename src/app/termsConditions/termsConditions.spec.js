describe('termsConditions section', function () {
    beforeEach(module('appMensajeria.termsConditions'));

    it('should have a dummy test', inject(function() {
        expect(true).toBeTruthy();
    }));
});